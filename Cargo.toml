[workspace]
resolver = "3"
members = ["src-tauri", "crates/sf-core", "crates/sf-mcp"]

# Read the optimization guideline for more details: https://tauri.app/concept/size/#cargo-configuration
[profile.release]
codegen-units = 1
lto = true
opt-level = 3
panic = "abort"
strip = true
